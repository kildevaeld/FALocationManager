source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'FALocationManager_Example', :exclusive => true do
  pod "FALocationManager", :path => "../"
end

target 'FALocationManager_Tests', :exclusive => true do
  pod "FALocationManager", :path => "../"

  pod 'Quick', '~> 0.3.1'
  pod 'Nimble'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
